/* global angular */
(function () {
	"use strict";

	var injectParams = ['Page'];
	var Controller = function (Page) {
		
	};

	Controller.$inject = injectParams;
	angular.module('lendApp').controller('ErrorController', Controller);
}());